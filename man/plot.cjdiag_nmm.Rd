% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot.cjdiag_nmm}
\alias{plot.cjdiag_nmm}
\title{Plot Nested Marginal Means Results}
\usage{
\method{plot}{cjdiag_nmm}(
  x,
  top_n = NULL,
  base_size = NULL,
  colors = NULL,
  palette = NULL,
  theme = NULL,
  label_wrap = NULL,
  attribute.names = NULL,
  level.names = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{cjdiag_nmm} object from \code{\link[=cj_fit]{cj_fit()}}}

\item{top_n}{Number of levels to display (default 25; NULL = all).}

\item{base_size}{Font size (default from global options or 12)}

\item{colors}{Named character vector overriding specific palette colors}

\item{palette}{Palette name: \code{"default"}, \code{"colorblind"}, or \code{"grey"}}

\item{theme}{A complete \code{\link[ggplot2:theme]{ggplot2::theme()}} object (overrides all theme defaults)}

\item{label_wrap}{Character width for label wrapping (default 35)}

\item{attribute.names}{Named character vector renaming attributes in display}

\item{level.names}{Named list for renaming levels}

\item{...}{Additional arguments passed to primary ggplot2 geom}
}
\value{
A ggplot object
}
\description{
Plots the cumulative percentage of choices explained by the top
attribute levels in order of decisiveness.
}
\examples{
\donttest{
df <- data.frame(
  y = sample(0:1, 200, TRUE),
  a = factor(sample(c("x","y"), 200, TRUE)),
  b = factor(sample(c("p","q","r"), 200, TRUE)),
  id = rep(1:100, each = 2)
)
nmm <- cj_fit(y ~ a + b, data = df, method = "nmm", resp_id = "id", n_boot = 0)
plot(nmm)
}
}
\seealso{
Other plotting: 
\code{\link{plot.cjdiag_crt}()},
\code{\link{plot.cjdiag_forest}()},
\code{\link{plot.cjdiag_importance}()},
\code{\link{plot.cjdiag_tree}()}
}
\concept{plotting}
