% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importance.R
\name{importance}
\alias{importance}
\title{Extract Importance Metrics from Fitted Model}
\usage{
importance(x, ...)
}
\arguments{
\item{x}{A fitted model object from \code{\link[=cj_fit]{cj_fit()}}}

\item{...}{Additional arguments (unused)}
}
\value{
A \code{cjdiag_importance} object (a list) containing:
\item{results}{Tibble with importance metrics at the fitted resolution}
\item{method}{Character: \code{"forest"} or \code{"tree"}}
\item{resolution}{Character: \code{"levels"} or \code{"attributes"}}
}
\description{
Extracts importance metrics from a fitted conjoint diagnostic model.
Returns the results at whatever resolution the model was fitted at:
level-specific (if \code{resolution = "levels"}) or attribute-level
(if \code{resolution = "attributes"}).
}
\examples{
\donttest{
df <- data.frame(
  y = sample(0:1, 200, TRUE),
  a = factor(sample(c("x","y"), 200, TRUE)),
  b = factor(sample(c("p","q","r"), 200, TRUE))
)
rf <- cj_fit(y ~ a + b, data = df, method = "forest")
imp <- importance(rf)
print(imp)
as.data.frame(imp)
}
}
\concept{results}
