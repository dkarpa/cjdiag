% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot.cjdiag_crt}
\alias{plot.cjdiag_crt}
\title{Plot CRT/HierNet Results}
\usage{
\method{plot}{cjdiag_crt}(
  x,
  type = "robustness",
  top_n = 25L,
  base_size = NULL,
  colors = NULL,
  palette = NULL,
  theme = NULL,
  label_wrap = NULL,
  attribute.names = NULL,
  level.names = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{cjdiag_crt} object from \code{\link[=cj_fit]{cj_fit()}}}

\item{type}{Plot type: \code{"robustness"} (default), \code{"survival"}, \code{"rank"},
\code{"mda"}, or \code{"cv"}. \code{"rank"} is a connected-dot plot of each level's
survival statistic (max \eqn{\lambda} at which the coefficient is
nonzero), ordered from most to least attended.}

\item{top_n}{Number of levels to display. Default \code{25} for \code{"robustness"},
\code{"mda"}, \code{"rank"}, and \code{"survival"}. Pass \code{NULL} to show every level.}

\item{base_size}{Font size (default from global options or 12)}

\item{colors}{Named character vector overriding specific palette colors}

\item{palette}{Palette name: \code{"default"}, \code{"colorblind"}, or \code{"grey"}}

\item{theme}{A complete \code{\link[ggplot2:theme]{ggplot2::theme()}} object (overrides all theme defaults)}

\item{label_wrap}{Character width for label wrapping (default 35)}

\item{attribute.names}{Named character vector renaming attributes in display}

\item{level.names}{Named list for renaming levels}

\item{...}{Additional arguments passed to primary ggplot2 geom}
}
\value{
A ggplot object
}
\description{
Plot CRT/HierNet Results
}
\examples{
\donttest{
# CRT requires the hierNet package
if (requireNamespace("hierNet", quietly = TRUE)) {
  df <- data.frame(
    y = sample(0:1, 200, TRUE),
    a = factor(sample(c("x","y"), 200, TRUE)),
    b = factor(sample(c("p","q","r"), 200, TRUE))
  )
  crt <- cj_fit(y ~ a + b, data = df, method = "crt",
                 lambda_grid = c(5, 10), n_folds = 2, n_perm = 2)
  plot(crt, type = "robustness")
}
}
}
\seealso{
Other plotting: 
\code{\link{plot.cjdiag_forest}()},
\code{\link{plot.cjdiag_importance}()},
\code{\link{plot.cjdiag_nmm}()},
\code{\link{plot.cjdiag_tree}()}
}
\concept{plotting}
