% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot.cjdiag_forest}
\alias{plot.cjdiag_forest}
\title{Plot Random Forest Results}
\usage{
\method{plot}{cjdiag_forest}(
  x,
  type = "importance",
  top_n = NULL,
  base_size = NULL,
  colors = NULL,
  palette = NULL,
  theme = NULL,
  label_wrap = NULL,
  attribute.names = NULL,
  level.names = NULL,
  group_by_attribute = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{cjdiag_forest} object from \code{\link[=cj_fit]{cj_fit()}}}

\item{type}{Plot type: \code{"importance"} (default), \code{"combined"}, \code{"rank"},
\code{"cumulative"} (MDA rank-decay with labels), or \code{"cumulative_pct"}
(cumulative \% of total MDA, area chart like NMM cumulative)}

\item{top_n}{Number of levels to display (default 25; NULL = all levels).
For \code{"cumulative"}, defaults to all levels if not specified.}

\item{base_size}{Font size (default from global options or 12)}

\item{colors}{Named character vector overriding specific palette colors}

\item{palette}{Palette name: \code{"default"}, \code{"colorblind"}, or \code{"grey"}}

\item{theme}{A complete \code{\link[ggplot2:theme]{ggplot2::theme()}} object (overrides all theme defaults)}

\item{label_wrap}{Character width for label wrapping (default 35)}

\item{attribute.names}{Named character vector renaming attributes in display}

\item{level.names}{Named list for renaming levels}

\item{group_by_attribute}{Group levels by attribute with visual separators (default \code{FALSE})}

\item{...}{Additional arguments passed to primary ggplot2 geom}
}
\value{
A ggplot object
}
\description{
Plot Random Forest Results
}
\examples{
\donttest{
df <- data.frame(
  y = sample(0:1, 200, TRUE),
  a = factor(sample(c("x","y"), 200, TRUE)),
  b = factor(sample(c("p","q","r"), 200, TRUE))
)
rf <- cj_fit(y ~ a + b, data = df, method = "forest")
plot(rf)
plot(rf, palette = "colorblind", base_size = 14)
plot(rf, type = "combined")
}
}
\seealso{
Other plotting: 
\code{\link{plot.cjdiag_crt}()},
\code{\link{plot.cjdiag_importance}()},
\code{\link{plot.cjdiag_nmm}()},
\code{\link{plot.cjdiag_tree}()}
}
\concept{plotting}
