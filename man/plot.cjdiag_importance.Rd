% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot.cjdiag_importance}
\alias{plot.cjdiag_importance}
\title{Plot Importance Results}
\usage{
\method{plot}{cjdiag_importance}(
  x,
  type = "mda",
  top_n = NULL,
  base_size = NULL,
  colors = NULL,
  palette = NULL,
  theme = NULL,
  label_wrap = NULL,
  attribute.names = NULL,
  level.names = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{cjdiag_importance} object from \code{\link[=importance]{importance()}}}

\item{type}{Plot type: \code{"mda"} (default), \code{"root"}, \code{"combined"}, \code{"cumulative"},
or \code{"cumulative_pct"} (forest only). For \code{nmm} objects, only \code{"mda"} and
\code{"cumulative"} are valid.}

\item{top_n}{Number of items to display (default 25; NULL = all levels).}

\item{base_size}{Font size (default from global options or 12)}

\item{colors}{Named character vector overriding specific palette colors}

\item{palette}{Palette name: \code{"default"}, \code{"colorblind"}, or \code{"grey"}}

\item{theme}{A complete \code{\link[ggplot2:theme]{ggplot2::theme()}} object (overrides all theme defaults)}

\item{label_wrap}{Character width for label wrapping (default 35)}

\item{attribute.names}{Named character vector renaming attributes in display}

\item{level.names}{Named list for renaming levels}

\item{...}{Additional arguments passed to primary ggplot2 geom}
}
\value{
A ggplot object
}
\description{
Plot Importance Results
}
\examples{
\donttest{
df <- data.frame(
  y = sample(0:1, 200, TRUE),
  a = factor(sample(c("x","y"), 200, TRUE)),
  b = factor(sample(c("p","q","r"), 200, TRUE))
)
rf <- cj_fit(y ~ a + b, data = df, method = "forest")
imp <- importance(rf)
plot(imp)
}
}
\seealso{
Other plotting: 
\code{\link{plot.cjdiag_crt}()},
\code{\link{plot.cjdiag_forest}()},
\code{\link{plot.cjdiag_nmm}()},
\code{\link{plot.cjdiag_tree}()}
}
\concept{plotting}
